services:
  - type: web
    name: careerpilot-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn careerpilot_backend.main:app --host 0.0.0.0 --port 10000